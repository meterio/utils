# Utility script used at meter

## Prerequisite

```bash
npm install
```

## Token Distribute

```bash
# ensure these before start using the script:
# a correct plan file at src/plan.csv 
# a env var DIST_ADMIN_PRIVKEY
# correct token address in src/distribute.js
node src/distribute.js
```